SELECT 
-- SQL Server, Postgres, Oracle, mySql

SELECT coluna1, coluna2
FROM tabela 

SELECT * //para todas
FROM tabela 

----//-- Aplicação
SELECT *
FROM person.Person;

SELECT Title
FROM person.Person;

SELECT *
FROM Person.EmailAddress;

--// Exercicio - extrair first name, last name

SELECT firstName, lastName
FROM Person.Person
